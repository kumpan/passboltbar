# CLAUDE.md

- Before starting work in a worktree, run `git fetch` and rebase or reset the branch onto the latest
  `origin/main`. Local `main` is often stale, and new worktrees can be cut from an old commit.
